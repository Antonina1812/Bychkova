#include <stdio.h>

int main(void) 
{
    int k, n = 1;
    scanf("%d", &k);
    if ((k == 0) || (k == 1))
        k = 1;
    for (int i = 2; (i <= k); i = i + 1)
        n *= i;
        printf("%d", n);
    return 0;
}
